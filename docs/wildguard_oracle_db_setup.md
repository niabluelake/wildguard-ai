# WildGuard AI Oracle DB 설정 문서

## 1. 목적

Oracle DB는 로그인 사용자 정보와 위험도 예측 기록을 저장하기 위해 사용한다.

---

## 2. 주요 테이블

- USERS
- RISK_PREDICTION_LOGS

향후 이미지/영상 탐지 기록과 상담 기록 저장 기능을 확장할 수 있다.

---

## 3. USERS 테이블 예시

```sql
CREATE TABLE users (
    user_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    email VARCHAR2(255) NOT NULL UNIQUE,
    password_hash VARCHAR2(255) NOT NULL,
    name VARCHAR2(100),
    created_at DATE DEFAULT SYSDATE
);
```

---

## 4. RISK_PREDICTION_LOGS 테이블 예시

```sql
CREATE TABLE risk_prediction_logs (
    log_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    user_id NUMBER,
    region_name VARCHAR2(100),
    location VARCHAR2(100),
    weather VARCHAR2(50),
    time_zone VARCHAR2(50),
    season VARCHAR2(50),
    predicted_score NUMBER,
    risk_grade VARCHAR2(20),
    main_risk_species VARCHAR2(100),
    message CLOB,
    created_at DATE DEFAULT SYSDATE,
    CONSTRAINT fk_risk_user FOREIGN KEY (user_id) REFERENCES users(user_id)
);
```

---

## 5. 주의사항

- DB 접속 정보는 GitHub에 업로드하지 않는다.
- 개인 PC와 학원 PC의 DB 환경이 다를 수 있으므로 설정 파일을 분리한다.
- 예측 기록 저장 실패가 위험도 예측 API 전체 실패로 이어지지 않도록 처리한다.
